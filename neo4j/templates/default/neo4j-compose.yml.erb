neo4j:
    image: neo4j
    container_name: neo4j
    ports:
        - "7474:7474"
    volumes:
        - "<%= ENV['HOME'] %>/neo4j/data:/data"
    ulimits:
        nofile:
            soft: 40000
            hard: 40000
